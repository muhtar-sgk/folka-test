import React from 'react'
import {View} from 'react-native'
import Colors from '../consts/Colors'

const Separator = () => {
    return (
        <View style={{height:1, backgroundColor: Colors.brownishGrey}}/>
    )
}

export default Separator